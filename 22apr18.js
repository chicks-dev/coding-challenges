//Make two functions that take in an array of numbers and return the smallest and largest number respectively





var min = function(list){

    return Math.min(...list);
}

var max = function(list){

    return Math.max(...list);
}
